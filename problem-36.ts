const make_shirt = (size: number | string, text: string) => {
    console.log(`the size of the t-shirt is ${size}`);
    console.log(`the text printed on t-shirt is ${text}`);
}

make_shirt("xl", "hello world");